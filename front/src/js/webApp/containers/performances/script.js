import React from "react";
import {useParams} from "react-router";


export const Script = () => {
    const { performance_id } = useParams()

    return (
        <>
            <div>staff</div>
        </>
    )
}